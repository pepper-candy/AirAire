# Activate Virtual Environment
venv_gpu\Scripts\activate

# Environment Setup
pip install -r requirements_new.txt

python -m src.train_gpu_v2 --output models/news_gpu_v2 --device cuda --resume 113

# Using FutuOpenD Terminal
FutuOpenD.exe -lang=en
python -m test.paper-trade-test