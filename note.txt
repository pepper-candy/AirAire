# Activate Virtual Environment
venv_gpu\Scripts\activate

# Environment Setup
pip install -r requirements.txt

python -m src.train_gpu_v2 --output models/news_gpu_v2 --device cuda --resume 113

# Using FutuOpenD Terminal
FutuOpenD.exe -lang=en
python -m test.paper-trade-test

# Folder Hosting on VM (run_finetune.bat)
src\__pycache__\
models\
logs\
data\

# Git Reset
git init
git remote add origin https://github.com/pepper-candy/AirAire/
git fetch origin
git reset origin/main